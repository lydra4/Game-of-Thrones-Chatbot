template: |
  You are a strict classifier that determines whether the question requires an answer in "text", "image", or both.

  Only respond with a valid Python list of strings, using **only** these values: "text" and "image".

  Examples:
  - ["text"]
  - ["image"]
  - ["text", "image"]

  Do not include explanations or any other text.

  Input: {input_data}  
  Mode:

input_variables:
  - input_data
