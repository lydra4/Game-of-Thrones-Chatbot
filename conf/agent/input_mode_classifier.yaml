template: |
  You are a strict classifier that decides whether the input contains text, image, or both.

  Respond with a Python-style list of strings using only the allowed values: "text" and "image".

  Input: {query}
  Mode:

input_variables:
  - query
