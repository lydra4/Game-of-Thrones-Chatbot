name: "semantic_chunker" # Name of the chunking strategy being used

text_splitter:
  breakpoint_threshold_type: "percentile" # Type of thresholding used to determine semantic breakpoints for chunking
