name: "recursive_character_text_splitter" # Name of the text splitter strategy being used

text_splitter:
  separators: ["\n\n"] # List of separators used to split the text hierarchically (e.g., by paragraphs)
  chunk_size: 500 # Maximum number of characters in each text chunk
  chunk_overlap: 50 # Number of overlapping characters between consecutive chunks
